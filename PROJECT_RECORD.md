# ParqScan 项目更新日志

按日期记录重要修改及原因。同一天内相关改动已合并，更早条目保持原顺序。

## 2026-09-01 — 记录详情画布、悬停预览与字段格描边

### 修改内容

- 详情画布改为普通 `QWidget`，删除内嵌 `QMainWindow`；每字段一格，新格叠在 `(0, 0)` 并置顶，已有格位置与大小不变。打开时快照源行。
- `ParquetDocument.close()` 关闭底层文件；分页临时文档与工作区关闭时释放句柄。
- 主表图片悬停改为 `ToolTip`，进入格子即显示并跟随指针；双击、打开详情或主窗失焦时关闭，避免盖住新窗口。
- 悬停图最长边 520 像素并与 72px 缩略图分缓存。
- 字段格保持 `DetailPreviewTile`、实心底；描边画在圆角路径上，不再用 QSS 矩形边框。

### 设计原因

内嵌 `QMainWindow` 会抢走最大化并干扰拖拽。`Popup` 悬停会触发假 Leave；主窗失焦后若仍按指针格子刷新，卡片会盖在详情上。格的 objectName 曾被改掉导致主题实心底未生效而透字。QSS `border` 按矩形描边，圆角 mask 裁切后会露出方框。

## 2026-08-06 — 修复文件搜索结果只计数、不在表格中显示

### 修改内容

- 文件搜索请求保存“完成后是否自动定位”的交互意图，异步线程结束后不再丢失该状态。
- 搜索完成后优先定位当前表格中已经可见的第一个匹配，并同步全文件结果游标。
- 当前页没有可见匹配且未启用筛选时，自动加载首个全文件匹配所在页面，并将目标单元格滚动到表格中央。
- 当前页筛选启用时，只选择筛选结果中已经可见的匹配；没有交集时保留筛选和当前页面，不进行隐式跨页跳转。
- 更新 UI 静态回归检查，覆盖搜索结果可见性与筛选状态隔离。

### 设计原因

上一版为了避免文件搜索完成后破坏当前页筛选，完全禁止了完成时的自动定位。这虽然消除了筛选重置，却导致搜索只能显示匹配数量，用户看不到任何结果。新的发布策略把“显示结果”和“改变筛选”拆开：能在当前视图显示就直接定位；没有筛选时允许跳到首个结果；有筛选时绝不为了定位而改变筛选上下文。

## 2026-08-06 — 修复文件搜索完成后重置当前页筛选

### 修改内容

- 新增独立的 `PageFilterState`，保存已应用的筛选列、运算符、查询文本和启用状态。
- 文件级搜索完成后只发布匹配结果与高亮，不再自动定位首个匹配。
- 删除文件搜索定位时自动清除当前页筛选的逻辑。
- 页面重载完成后，根据独立筛选状态重新应用当前页筛选。
- 当文件搜索目标被当前页筛选隐藏时显示明确提示，不再静默恢复未筛选页面。
- 新增中英文提示及静态回归检查。

### 设计原因

旧实现虽然隔离了文件级搜索和当前页搜索的结果状态，但文件搜索完成回调仍会自动定位首个结果。该定位可能触发分页加载，或者在目标被筛选隐藏时主动清除筛选，因此异步扫描结束后会把用户刚应用的筛选视图恢复为文件初始状态。搜索结果发布、搜索导航和页面筛选现在保持正交：扫描完成不改变视图，导航不修改筛选，分页只从保存的筛选条件重建当前页可见行。

## 2026-08-06 — 隔离文件级搜索与页内搜索状态

### 修改内容

- 记录详情工具栏删除自定义最大化按钮，继续保留系统标题栏原生最大化能力。
- 数据预览新增“文件级搜索 / 当前页搜索”显式范围选择器。
- 两种搜索分别保存查询、结果、游标和待跳转坐标，不再共享单一状态。
- 文件搜索线程使用请求代次校验，范围切换、查询变化或新请求启动后，旧线程不能回写覆盖当前结果。
- 当前页搜索只扫描当前模型的可见偏移，尊重页内过滤，不跨页且不清除过滤。
- 翻页时清理页内搜索旧坐标和页内过滤输入；文件搜索仍按原始行坐标跨页定位。

### 设计原因

旧实现只按查询文本判断异步结果是否有效。同一文本在文件级与页内范围之间切换时，晚到的文件线程可能覆盖页内结果；文件搜索定位被过滤隐藏的单元格时又会只清模型过滤而不清界面输入。新实现用范围隔离状态和请求代次共同解决结果覆盖、游标错位、高亮残留及界面状态不同步。

## 2026-08-06 — 记录详情选区同步高亮

### 修改内容

- 记录详情中的主字段文本、整行 JSON、并排字段文本和 Hex 文本统一接入选区监听。
- 用户在任一可见详情文本中选中字符串后，当前窗口所有可见详情文本中的完全相同字符串会同步高亮。
- 匹配采用区分大小写的精确文本查找，取消选择或只选择空白时会清除全部附加高亮。
- 并排字段增删和详情标签切换后会重新计算当前可见内容，避免隐藏页面保留陈旧高亮。
- 单次最多绘制 5000 个匹配，防止超大 JSON 中选择单字符时阻塞界面。
- 更新中英文操作提示，并新增静态界面测试覆盖信号连接、精确匹配、可见性过滤与并排预览接入。

### 设计原因

记录详情经常包含重复字段名、枚举值或结构键。仅保留系统默认的单处选区无法快速判断同一字符串在当前记录中的全部位置，因此需要在不改变原始文本和当前选区的前提下，使用临时附加格式同步标记所有可见匹配。

## 2026-07-27 — 格式化记录详情中的字符串 JSON

### 修改内容

- 为 `parqscan/utils/serialization.py` 增加字符串 JSON 容器识别：只有整段内容是合法对象或数组时才解析。
- `pretty_text()` 现在会将以字符串保存的 JSON 对象和数组按层级缩进展示。
- `RecordDetailDialog` 的普通字段展示继续使用统一格式化入口，因此无需改变表格模型或导出数据语义。
- 无效 JSON、普通文本以及 `true`、`123`、`"text"` 等 JSON 标量保持原样。
- 新增 `tests/test_serialization.py`，覆盖嵌套对象、嵌套数组、中文、BOM 与空白、无效 JSON、JSON 标量和原生容器。
- 更新 `PROJECT_OVERVIEW.md`，补充展示链路、函数职责、当前状态和行为边界。

### 设计原因

Parquet 数据中的 JSON 字段经常以字符串形式保存。原实现只会格式化已经是 Python `dict/list/tuple` 的值，字符串 JSON 会作为一行原文显示，导致嵌套对象和数组难以阅读。本次修改在完整验证后进行纯展示层格式化，既满足分级展开需求，又避免更改原始数据、表格预览和导出结果。

## 2026-07-27 — 修复冻结程序无法启动与 Windows 图标缺失

### 修改内容

- Windows 默认改为 `onedir`，并保留 `python build.py --onefile` 的单文件模式。
- 构建改为执行受版本管理的 `ParqScan.spec`，避免命令行构建覆盖手工维护的图标与 DLL 配置。
- 新增 Windows 多尺寸 `ParqScan.ico` 和运行时 PNG 回退资源。
- 在 EXE 配置中传入 `.ico`，并显式收集 `PySide6.QtSvg`。
- 在 Conda 环境中额外收集 PyArrow 位于 `Library/bin` 的动态库依赖。
- `main.py` 改为延迟导入 GUI 和数据依赖，在 `noconsole` 模式下先恢复空标准流。
- 启动异常写入 `ParqScan/logs/startup.log`，Windows 上同时显示原生错误消息框。
- 构建完成后自动运行打包产物的 `--smoke-test`，验证 Qt、依赖和资源初始化。

### 设计原因

原配置只调用 `application.setWindowIcon()`，这只能设置运行中的窗口/任务栏图标，不能修改 Windows PE 文件图标；PyInstaller 的 EXE 配置也明确使用了空图标。与此同时，`--noconsole` 会隐藏标准错误输出，任何导入错误或 DLL 加载失败都会表现为双击后无反应。用户环境为 Anaconda，PyArrow 的原生共享依赖可能位于环境的 `Library/bin`，需要通过 Conda-aware 动态库收集补齐。

## 2026-07-27 — 修复 PyInstaller 多 Qt 绑定冲突

### 修改内容

- 移除 `build.py` 中无过滤的 `--collect-submodules pyarrow`，改为使用 PyInstaller 官方 PyArrow hook。
- 显式排除 PyQt5、PyQt6、PySide2、PyArrow/Pandas 测试包及测试文档工具。
- 构建子进程固定 `QT_API=pyside6`，确保 Conda 多 Qt 环境中的绑定选择稳定。
- 将 `ParqScan.spec` 改为项目相对路径，并同步相同的排除策略。
- 新增 `tests/test_build.py`，覆盖 PyArrow 收集策略、Qt 绑定排除、环境变量和三平台输出模式。

### 设计原因

原构建命令在官方 PyArrow hook 之外再次全量收集所有 `pyarrow` 子模块，导致 `pyarrow.tests` 引入 pytest、IPython、Sphinx、Matplotlib 等开发依赖，并进一步触发 PyQt5 hook。PyInstaller 不允许同一产物同时收集 PySide6 与 PyQt5，因此构建被中止。

## 2026-07-22 — 从零完成项目主体

### 修改内容

- 根据 `ParqScan.md` 创建完整项目结构。
- 实现 PySide6 主窗口、多文件工作区、菜单、拖拽、命令行打开和最近文件。
- 实现 Parquet Schema、物理类型、压缩、存储占比和行组统计。
- 实现二进制图片识别、缩略图、悬停预览、Hex、Base64 和附件导出。
- 实现 CSV、JSON、Excel、Markdown、SQL 和 Parquet 碎片导出。
- 实现图片批量提取、后台进度、取消、主题、双语和跨平台打包。

### 设计原因

项目此前只有需求文档，需要形成可运行、可测试、可打包和可维护的完整桌面应用。

## 2026-07-22 — 修复全量累积和嵌套图片

### 修改内容

- 删除初始大快照、顺序 `fetchMore()`、Arrow Table 持续拼接和“加载全部”路径。
- 将读取改为后台范围读取并限制页面缓存。
- 根据真实 Schema `struct<bytes: binary, path: string>` 增加嵌套二进制类型和值递归。
- 图片缩略图改为 QImageReader 目标尺寸解码。
- 元数据只统计真实 binary 叶子压缩大小。
- 图片、Hex、Base64、附件导出和批量提取统一使用嵌套真实载荷。

### 设计原因

旧实现会随滚动不断增加内存，并错误假设图片字段本身就是顶层 binary，导致真实图片无法显示。

## 2026-07-23 — 重构为固定分页和统一现代化设计系统

### 修改内容

- 将数据浏览从“可见区自动页缓存”改为用户可理解的固定分页模式。
- 新增 20、50、100、200 四种每页行数选项，默认 20 项，并持久化用户选择。
- 模型只保留当前页一个 Arrow Table；翻页或改变页大小会取消旧任务并释放旧数据与缩略图。
- 新增首页、上一页、页码输入、下一页、末页和总页数展示。
- 新增 `ParquetDocument.read_rows()` 真实扫描进度回调。
- 新增页任务进度信号节流，只在百分比变化时更新 GUI。
- 新增表格骨架占位、半透明加载遮罩、居中加载卡和自定义进度条。
- 加载期间锁定分页控件，避免重复点击堆积读取任务。
- 新建 `design_system.py`，提供卡片、指标卡、分页器、加载遮罩和空状态组件。
- 新建 QPainter 图标工具，替代主要系统原生图标。
- 重构浅色与深色 QSS，统一菜单、按钮、输入框、下拉框、页码框、表格、树、列表、标签页、详情窗口、进度条和状态栏。
- 单独重绘垂直和水平滚动条，取消系统箭头并使用透明轨道与圆角滑块。
- 重构元数据页为文件标题、路径卡、六个指标卡和两个详情标签页。
- 重构数据页为标题区、搜索卡、筛选卡、表格卡和分页底栏。
- 双击行为从“只处理二进制单元格”扩展为“任意单元格打开完整记录详情”。
- 新建 `RecordDetailDialog`：左侧切换整行字段，右侧展示完整文本、格式化对象、真实图片或 Hex + ASCII。
- 详情窗口增加字段类型、源行号、内容大小、整行 JSON、完整值复制和二进制导出。
- 整行 JSON 改为首次打开标签页时延迟生成，避免详情窗启动时处理大附件。
- 图片行不再在页面加载完成后批量检查；只有可见缩略图真实解码成功时才增高对应行。
- Ctrl+F 改为后台扫描整个文件，保存全局匹配坐标并支持跨分页前后跳转。
- 修正快速翻页时旧任务对象的生命周期，旧代次结果会被丢弃且任务引用能正常释放。
- 更新 README、项目总览、语言文件和自动化测试。

### 设计原因

用户明确要求一次加载一页、提供可见加载进度、改善低速加载时的感知体验、全面摆脱原生控件的粗糙视觉，并为被表格截断的超长数据提供一致且完整的二级详情窗口。本次修改围绕“确定性分页、明确反馈、统一设计语言、完整数据查看”重新设计了数据页和整个应用界面。

## 2026-07-23 — 主动优化：进度事件和详情启动成本

### 修改内容

- 页面读取进度按照实际需要扫描到的行组位置计算，而不是按整个命中行组大小粗略估算。
- 工作线程只在读取百分比变化时发送进度，避免大行组产生大量信号。
- 图片行高改为由可见缩略图解码成功信号驱动，不在页加载完成时提前扫描或解码整页图片。
- 页面读取的内部批次下限收紧到 64 行，降低大图片页的瞬时内存。
- 完整图片只在用户双击详情后解码。
- 整行二进制 Base64 只在用户主动打开整行 JSON 标签时生成。

### 设计原因

这些改动不改变用户要求的功能，但能降低进度更新、缩略图预热和详情窗口启动带来的额外阻塞，使慢文件的体验更稳定。

## 2026-07-23 — 根据真实截图修复视觉污染与控件细节

### 修改内容

- 修复全局 `QWidget` 背景规则污染 `QLabel` 的问题，消除标题、指标、路径和详情信息后方的重复浅色横条。
- 将普通 QWidget 与 QLabel 改为透明背景，只对窗口、卡片、表格、菜单等明确容器设置表面颜色。
- 左侧文件列表关闭横向滚动，长文件名中间省略并保留完整路径 Tooltip。
- 主分割器手柄收紧为单像素，侧栏宽度和内部间距重新调整。
- 全局横纵滚动条由 12 像素收紧到 8 像素，取消系统箭头和按钮，调整滑块最小长度及悬停反馈。
- 分页页码从 QSpinBox 替换为带 QIntValidator 的 QLineEdit，移除原生上下箭头。
- 分页导航新增独立紧凑容器，页码、总页数、首页、前后页和末页形成统一视觉组。
- 元数据标题和路径改为可省略标签；路径卡增加复制完整路径按钮。
- 指标卡增加蓝色强调条并收紧留白，改善信息层级。
- 数据表根据字段语义选择正文伸缩列，解决少列数据表右侧大片空白。
- 数据表、Schema 表和行组树使用按像素滚动，并统一更紧凑的表头与行高。
- 详情窗口的文件名、字段名、类型和图片信息支持省略与 Tooltip，避免长内容挤压操作按钮。
- 详情文本新增“自动换行”切换，默认保留原始文本布局，用户可按内容类型选择查看方式。
- 新增 UI 静态回归测试，防止标签背景污染、粗滚动条和原生 SpinBox 再次出现。
- 更新中英文语言文件、README 和项目总览。

### 设计原因

真实运行截图显示上一轮设计系统虽然统一了颜色与卡片，但全局 QSS 选择器错误地给所有 QWidget 子类绘制背景，造成多处文本后方出现浅色条；侧栏长文件名触发了不必要的横向滚动条；分页仍暴露系统 SpinBox 微调按钮；少列数据表和指标卡的信息密度也不理想。本轮修改从控件继承、布局尺寸、滚动策略和信息层级四个方面修复这些可见问题，而不是继续叠加局部颜色补丁。


## 2026-07-23 — 修复翻页方向、提示浮层和页面布局一致性

### 修改内容

- 重写左右、首页和末页图标的坐标计算，确保下一页始终向右、上一页始终向左，首页和末页的竖线位于正确一侧。
- 新增 `HintToolButton`，图标按钮不再依赖平台原生 Tooltip，而是显示应用控制的紧凑提示标签。
- 紧凑提示加入延迟显示、离开隐藏、屏幕边界约束、无鼠标拦截和无障碍名称。
- 分页导航改为独立按钮样式，增强正常、悬停、按下和禁用状态的区分。
- 分页摘要改为显示当前实际行范围与文件总行数，总页数改为清晰的本地化文本。
- 页面加载完成后自动恢复表格垂直顶部和水平起点，避免继承上一页滚动位置。
- Schema 表将逻辑类型列设为弹性列，并为其他统计列分配稳定宽度，修复最后一列被截断的问题。
- 行组详情表同步调整列宽策略，使名称列吸收剩余空间，统计列保持可读。
- 二进制检查页将模式、当前数据摘要、搜索框和前后跳转整合到统一工具卡。
- Hex 前后跳转按钮改为紧凑图标按钮，并复用新的方向正确图标和提示组件。
- 调整分页底栏、Hex 编辑器、状态栏和提示标签的浅色及深色样式。
- 新增 UI 静态回归检查，覆盖图标方向、自定义提示、翻页滚动复位和元数据列布局。
- 更新中英文分页范围与总页数文本、项目总览和维护记录。

### 设计原因

最新运行截图显示，原图标坐标翻转逻辑将左右箭头实际画反；平台原生 Tooltip 又把“下一页”等短提示渲染成过大的浮层。此外，翻页后表格可能保留上一页滚动位置，Schema 最后一列也会在常规窗口中被截断。本次修改从图标几何、提示生命周期、分页信息结构、滚动状态和表格列宽策略五个层面修复这些问题，并同步收紧二进制检查页的信息层级。

## 2026-07-23 — 完善二进制检查、滚动条交叉角与搜索控件

### 修改内容

- 明确二进制检查页用于查看当前选中 binary 或嵌套 binary 单元格的原始字节，而不是替代图片详情窗口。
- 二进制检查页新增自动、Hex 字节、文本和偏移地址四种搜索模式。
- 搜索逻辑从 `QPlainTextEdit.find()` 改为直接扫描原始 bytes，并将匹配结果映射回 Hex 转储字符位置。
- 新增匹配计数、当前匹配序号、真实字节偏移、无匹配和格式无效反馈。
- 未选择二进制单元格时使用独立空状态，搜索控件同步禁用。
- 数据预览搜索框改为 `SearchField` 复合控件，搜索图标与输入文本分离绘制，修复 `QLineEdit.addAction()` 在 Windows 下显示成异常圆点的问题。
- 翻页按钮改为图标加可见文字，不再显示任何浮动 Tooltip。
- Hex 前后匹配按钮同步改为图标加文字，避免仅靠悬浮说明。
- 数据表增加显式 `ScrollCorner`，并为 `QAbstractScrollArea::corner` 设置主题背景，消除横纵滚动条交叉处的正方形异物。
- 密集数据表与分页底栏取消假圆角，工具卡和指标卡统一为较小圆角，解决子控件无法跟随父卡片裁切导致的异常边角。
- Windows 弹出菜单取消窗口级圆角，仅保留菜单项轻量圆角，避免原生窗口四角出现未裁切白块。
- 浅色和深色主题同步更新搜索框、分页按钮、Hex 工具栏、滚动角和菜单样式。
- 更新中英文语言文件、项目总览和 UI 静态回归测试。

### 设计原因

实际运行截图表明，原二进制页只在已经格式化的转储文本中搜索，用户无法判断搜索目标和支持格式；平台 Tooltip、QLineEdit action、父卡片圆角以及默认滚动区域角部又分别产生了巨大提示框、异常搜索图标、假圆角和正方形交叉块。本次修改把这些问题统一归因到控件职责和设计系统边界，并从数据搜索逻辑与控件结构两层修复。


## 2026-07-23 — 移除常驻二进制标签并统一行级交互

### 修改内容

- 删除主工作区中的常驻“二进制检查”标签，只保留元数据与数据预览两个高频标签。
- 数据表改为整行单选；单击任意单元格即可选中对应记录，同时状态栏仍显示实际点击列。
- 双击任意单元格继续打开整行完整详情，不再要求用户点击 image 字段。
- 新增 `_binary_index_for_row()`：右键任意位置时优先使用当前列，否则自动寻找该行首个有效二进制字段。
- 数据表右键菜单新增“查看原始字节”，并保留 Base64 复制与原始文件导出。
- 删除数据页到常驻 Hex 标签的 `binary_selected` 同步链路。

### 设计原因

原来的二进制标签占用主导航空间，却只服务少数高级场景；同时二进制数据与具体字段绑定，不适合作为整份文件的常驻页面。将其移入详情和右键菜单后，普通用户只面对元数据和数据浏览，技术用户仍能在需要时检查真实 bytes。

## 2026-07-23 — 修复原始字节搜索交互与匹配语义

### 修改内容

- 原始字节搜索增加明确的“搜索”按钮，并保留回车作为快捷方式。
- 输入或切换模式后显示“点击搜索开始定位”，不再让用户误以为输入即自动执行。
- 自动模式将 `FF`、`FFD8FF` 等偶数长度纯十六进制字符串识别为 Hex。
- 搜索直接扫描原始 bytes，不再依赖格式化转储文本。
- 匹配扫描改为每次推进一个字节，保留重叠匹配。
- 搜索结果显示当前序号、总数和真实字节偏移，并映射回 Hex 转储高亮。
- 上一个和下一个按钮使用固定宽度可见文字，不依赖 Tooltip。

### 设计原因

旧界面没有显式搜索动作，且自动模式会把 `FF` 误判为普通文本，导致常见图片文件头查询结果不符合用户预期。新实现把搜索触发、解析规则、匹配范围和结果反馈全部显式化。

## 2026-07-23 — 完成真实圆角弹层、图标分页与图片自适应

### 修改内容

- 新增 `RoundedMenu`，对 QMenu 顶层窗口应用圆角蒙版，消除菜单四角方形白块。
- 新增 `RoundedComboBox`，使用自定义 QListView 并裁切下拉 popup，统一筛选、页大小和 Hex 搜索模式下拉列表。
- 新增 `RoundedTableView`，对包含 viewport 和滚动条的完整表格进行真实圆角裁切。
- 分页按钮改为纯图标固定尺寸，不再放置“上一页/下一页”文本，因此不会出现 `...`。
- 分页文字只保留为无障碍名称和状态栏提示，不显示浮动 Tooltip。
- 新增 `ResponsiveImageLabel`，记录详情和图片预览窗口会随画布尺寸等比缩放图片，并关闭不必要的横纵滚动条。
- 禁用数据表平台原生长文本 Tooltip，完整内容统一由详情窗口承载。
- 新增应用内 `MessageDialog` 替代 QMessageBox，并新增 `ItemSelectionDialog` 替代图片列选择用的 QInputDialog。
- 浅色与深色主题同步增加真实圆角菜单、下拉弹层、消息窗口、表格和图片画布样式。

### 设计原因

Qt 的菜单、下拉列表和复杂滚动视图是独立或复合窗口，仅设置 QSS 圆角不会裁切子窗口，因此会留下截图中的直角、白块和混合原生风格。通过蒙版和自定义组件统一窗口边界，才能让视觉规则真实生效。图片默认适应窗口也能避免用户为了查看完整图表频繁拖动滚动条。

## 2026-07-23 — 主动维护：清理无效导入并补充 UI 回归检查

### 修改内容

- 删除 `application.py`、`range_export_dialog.py` 与 `export_service.py` 中未使用的导入。
- 更新 `tests/test_ui_assets.py`，覆盖无常驻二进制标签、整行选择、显式原始字节搜索、图标分页、真实圆角 popup、图片自适应和禁用长文本 Tooltip。
- 更新 README 与 PROJECT_OVERVIEW，确保新对话可以直接理解当前交互和架构。

### 设计原因

本次重构改变了主导航和设计系统边界，需要通过静态回归检查防止后续重新引入原生弹层、被截断分页文字或错误的 Hex 自动识别。未使用导入会降低代码可读性，因此单独记录清理。

## 2026-07-23 — 修复消息窗口留白与分页数量裁切

### 修改内容

- 重构应用内消息窗口尺寸策略，标题、说明文字和图标均使用内容驱动的固定高度，不再被布局额外拉伸。
- 消息卡片与根布局启用内容尺寸约束，关于窗口会根据实际文字自动计算高度。
- 消息正文增加明确宽度边界，在高 DPI 和系统字体缩放环境中保持稳定换行。
- “每页”数量选择器宽度调整为可完整容纳三位数选项。
- `RoundedComboBox` 改为显式绘制当前值，避免系统下拉按钮在不同 DPI 下侵占文本区域并把 `20` 裁成 `2`。
- 浅色、深色主题同步增加分页数量选择器的专用宽度、内边距和字重规则。
- 新增消息窗口与分页数量选择器的静态回归检查。

### 设计原因

实际运行截图显示，Qt 在特定 Windows DPI 与字体缩放组合下会把可换行标签分配到多余垂直空间，同时下拉按钮的原生标签区域会压缩当前值。两处问题都不能仅靠扩大窗口解决，因此分别改为内容尺寸约束和稳定的自定义文本绘制。

## 2026-07-23 — 恢复关于页面品牌 Logo 并增加图片交互缩放

### 修改内容

- `MessageDialog` 新增可选应用图标参数，并为品牌 Logo 使用独立的 `MessageLogo` 样式。
- 关于页面改为使用主窗口持有的 ParqScan 应用图标和完整“关于 ParqScan”标题，不再显示通用信息或警告符号。
- 新增 `ZoomableImageView`，基于 QGraphicsView 保存完整图片并支持适应窗口、滚轮缩放、按钮缩放和拖拽平移。
- 记录详情图片区域新增“缩小”“放大”“恢复视图”按钮以及实时缩放百分比。
- 独立图片预览窗口同步使用相同缩放画布和控制按钮，保持两处交互一致。
- 新增放大、缩小和恢复视图程序化图标。
- 中英文语言文件新增图片缩放控制文本。
- 浅色与深色主题新增品牌 Logo 和可缩放图片画布样式。
- UI 回归检查新增关于页面应用 Logo、滚轮缩放、按钮缩放和恢复视图验证。

### 设计原因

关于窗口属于品牌信息页面，使用通用状态符号会让用户误以为这是提示或警告窗口，也会丢失应用识别。记录详情中的图片仅自动适配无法检查局部细节，因此需要在保留默认完整视图的同时提供明确、可发现的缩放和恢复操作。

## 2026-07-23 — 修复详情原图质量与下拉框文本截断

### 修改内容

- 新增 `OriginalImageItem`，直接保存并绘制完整解码后的 `QImage`。
- `ZoomableImageView` 改为以完整 QImage 为唯一图像源；适应窗口、滚轮和按钮缩放只修改视图矩阵。
- 记录详情与独立图片预览改用 `set_source_image()`，不再经过任何预缩放 QPixmap 中间路径。
- 保留表格缩略图的目标尺寸解码，但明确隔离缩略图缓存与详情原图链路。
- 重写 `RoundedComboBox.paintEvent()` 的文本区域计算，不再依赖平台返回的 `SC_ComboBoxEditField`。
- 当前选项仅在真实宽度不足时省略，修复“包含”“等于”和页大小选项显示为 `...` 或单个字符的问题。
- 页大小选择器宽度收紧，下拉弹层按最长选项实际宽度展开。
- 修正浅色与深色主题中的页大小宽度规则。
- 新增完整原图渲染和紧凑下拉值绘制的 UI 回归检查。

### 设计原因

实际运行截图表明，用户无法确认详情页是否仍在使用缩略图，同时 Windows 在特定 DPI 和字体组合下会返回异常狭窄的 ComboBox 编辑区域，导致明明足够宽的控件仍把短文本裁成省略号。本次修改从数据源和绘制几何两端消除歧义：详情页只保留完整原图，控件则直接基于自身内容矩形绘制当前值。
